# Relaxed Filter Configuration
# Use these values to catch more alpha transactions
# ⚠️ Warning: May increase false positives (transfers, spam)

# === Alpha Signal Discipline (RELAXED) ===
DUST_SOL_SPENT=0.0001                    # Lowered from 0.001 (10x more sensitive)
MIN_ALPHA_TOKEN_BALANCE=0.0000001        # Lowered from 0.000001 (10x more sensitive)
MIN_SIZE_INCREASE_RATIO=0.1              # Lowered from 0.25 (catches 10%+ increases)
MAX_SIGNAL_AGE_SEC=60                    # Keep at 60 seconds
MAX_ALPHA_ENTRY_MULTIPLIER=2             # Keep at 2x

# === How to Use ===
# 1. Copy these values to your .env file
# 2. Restart the bot: pm2 restart alpha-snipes-paper --update-env
# 3. Monitor logs for 24-48 hours
# 4. Adjust if you see too many false positives

