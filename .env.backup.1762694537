# Telegram Bot Configuration
TELEGRAM_TOKEN=7942901226:AAFfTlsJ64k-o6a4rPkTe2ZRzpFmgwVTmm0
COMMAND_CHAT_ID=1368896735
ADMIN_USER_ID=1368896735

# Using numeric chat ID from @RawDataBot
TELEGRAM_CHAT_ID=-1003291954761

# === Solana RPC & Wallet ===
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=a40f7eab-38c9-4097-8a72-f851b261e27d
WALLET_PRIVATE_KEY=your_base58_private_key_here

# === Alpha Wallet to Watch ===
ALPHA_WALLET=8zkJmeQS1J3GUkPvfboeT76bwojADU6dyTZsCBiMdCVp

# === Trade Size ===
BUY_SOL=0.01
EARLY_TP_PCT=0.3
TRAIL_STOP_PCT=0.2

# === Priority fee (Jito-lite) ===
CU_UNIT_PRICE_MICROLAMPORTS=5000      # ~0.005 SOL per 1M CU, tune 2k–10k
CU_LIMIT=800000                       # upper bound on compute units

# === Rug/safety ===
REQUIRE_AUTHORITY_REVOKED=true
MAX_TAX_BPS=500                       # 5% max (rough heuristic via quotes)
MAX_PRICE_IMPACT_BPS=3000             # 30% for our buy size
SENTRY_WINDOW_SEC=120                 # monitor 2 minutes after entry
SENTRY_MAX_DRAWDOWN_PCT=0.22          # exit if -22% from entry during sentry

# === Trading Mode ===
# paper = simulation mode (no real transactions, no wallet key needed)
# live = real trading mode (requires WALLET_PRIVATE_KEY)
TRADE_MODE=paper

# === Debug / Diagnostics ===
# Enable extra logging to explain why a tx did NOT produce a signal/buy
DEBUG_TX=true
# Also echo debug logs into the Telegram command chat
DEBUG_TO_TELEGRAM=false

# === Networking hardening ===
# Optional: override Jupiter base (keeps fallback list too)
# JUP_QUOTE_BASE=https://quote-api.jup.ag/v6/quote
# Optional: comma-separated DNS servers used by Node (overrides system DNS)
# DNS_OVERRIDE=1.1.1.1,1.0.0.1,8.8.8.8,8.8.4.4
